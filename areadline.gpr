project Areadline is

  for Main use ("test_read_line");

  package Builder is
    for Default_Switches ("Ada") use ("-g");
  end Builder;

  package Compiler is
    for Default_Switches ("Ada") use ("-g", "-O2", "-gnatg", "-gnaty", "-gnatwa");
  end Compiler;

end Areadline;
